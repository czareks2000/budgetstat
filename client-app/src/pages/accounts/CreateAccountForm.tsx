import { Box, Paper } from "@mui/material"
import { observer } from "mobx-react-lite"

export default observer(function CreateAccountForm() {
  return (
    <Paper>
        <Box height={600}>

        </Box>
    </Paper>
  )
})
